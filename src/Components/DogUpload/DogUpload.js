import React, { Component } from "react";

export default class DogUpload extends Component {
  constructor(props) {
    super(props);

    this.state = {};
  }

  render() {
    return (
      <div>
        <h1>DogUpload</h1>
      </div>
    );
  }
}
